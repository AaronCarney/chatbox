## Session Directive
> **MANDATORY:** This progress file defines your assignment. Do NOT interpret, summarize, or re-scope.
> Resume work exactly where the previous session left off. Follow the What's Next section literally.

## Application Context
- ChatBridge — K-12 AI chat platform with third-party app integration (chess, go, spotify). GauntletAI take-home sprint, 1-week deadline (final Sunday 2026-04-06 11:59 PM CT).
- Current goal: Smoke testing + deployment. All 38 implementation tasks COMPLETE.

## Position
- Project: ChatBridge, repo: `/home/context/projects/chatbridge` (symlinked at `projects/chatbridge`), branch: `main`, phase: **implementation** (post-build, smoke testing)

## L2 Context
- L2 plan: `docs/plans/2026-04-02-chatbridge-l2.md` (38 tasks, 6 waves, all COMPLETE)
- All 38 tasks implemented across 6 waves using parallel-plan-executor
- 84 server tests passing across 11 test files
- Frontend builds successfully (Vite, 25s)

## Task State (JSON)
```json
{
  "tasks_done": ["T1-T38 (all 38 tasks)"],
  "tasks_remaining": [],
  "test_baseline": {"suite": "vitest", "passing": 84, "files": 11},
  "planning_status": "complete",
  "implementation_status": "complete — all 38 tasks merged to main",
  "smoke_test_status": "in progress — server runs, DB connected, Clerk enabled, apps seeded. Need: Spotify keys, full frontend+backend integration test, deployment"
}
```

## What's Next
1. **Get Spotify credentials from user** — user hasn't provided SPOTIFY_CLIENT_ID/SECRET yet. Need these for `.env`.
2. **Full integration smoke test** — start both server (`npx tsx server/src/boot.ts`) + frontend (`npx vite --config vite.config.ts`), verify:
   - Frontend loads at localhost:5173
   - Clerk sign-in works
   - Chat sends message to OpenAI and streams response
   - Chess app loads in iframe
   - Tool calling triggers app launch
3. **Fix integration bugs** — likely issues with ChatBridgeApp wiring, SSE parsing, postMessage comms
4. **Push to GitHub** — `git push origin main` to `AaronCarney/chatbox`
5. **Deploy** — Vercel (frontend) + Railway (server). User's website is aaroncarney.me
6. **Demo video** — 3-5 min (user records)
7. **Social post** — X/LinkedIn (user posts)

## Docker Infrastructure
- PostgreSQL: `docker start chatbridge-postgres` (port 54328, user: postgres, pass: postgres, db: chatbridge)
- Redis: `docker start chatbridge-redis` (port 63790)
- Schema applied, 3 apps seeded (chess, go, spotify)

## Environment
- `.env` configured with: OpenAI key (real), Clerk keys (real), DB URL (postgres://postgres:postgres@127.0.0.1:54328/chatbridge), Redis URL (redis://localhost:63790)
- Missing: SPOTIFY_CLIENT_ID, SPOTIFY_CLIENT_SECRET (user hasn't provided yet)
- Server entry point: `server/src/boot.ts` (loads dotenv before ESM imports)
- Dev command: `cd server && npx tsx src/boot.ts` for server, `npx vite` for frontend

## Key Implementation Notes
- Clerk auth is dynamic-imported: skips entirely when keys < 20 chars (dev mode)
- Server uses pino structured logging with request middleware, child loggers per chat request
- Frontend is Chatbox fork stripped to web-only Vite SPA
- ChatBridgeApp.tsx is the convergence component wiring all hooks
- PostMessage protocol: CHATBRIDGE_V1 envelope format

## Key Files
- Server entry: `server/src/boot.ts` → `server/src/index.ts`
- Chat route (full pipeline): `server/src/routes/chat.ts`
- Frontend app: `src/renderer/components/ChatBridgeApp.tsx`
- Logger: `server/src/lib/logger.ts`
- Auth (optional Clerk): `server/src/middleware/auth.ts`
- Design spec: `docs/specs/2026-04-02-chatbridge-design.md`
- L2 plan: `docs/plans/2026-04-02-chatbridge-l2.md`

## Git State
```
Latest commits on main:
e9ef0d0 fix: dotenv boot ordering, Clerk + DB connected
d79fdaf fix: smoke test fixes — Clerk optional, dotenv, server keepalive
377a001 feat: add pino structured logging + fix build issues
... (38 implementation task commits across 6 waves)
```

## Constraints/Blockers
- Spotify OAuth requires SPOTIFY_CLIENT_ID and SPOTIFY_CLIENT_SECRET (user hasn't provided)
- User's website: aaroncarney.me (for deployment/linking)
- No GitLab repo created yet (PRD requires GitLab, have GitHub fork at AaronCarney/chatbox)
- Demo video + social post are user tasks
